export const getModules = () => ({
  toolbar: [
    ['bold', 'italic', 'underline', 'strike'],
    [{ list: 'ordered' }, { list: 'bullet' }]
  ]
});

export const getFormats = () => ['bold', 'italic', 'underline', 'strike', 'list', 'bullet'];
